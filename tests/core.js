const chai = require('chai');
const expect = chai.expect;

describe('API', function() {

  // todo - write a test that ensures all webhooks have a unique set of
  // properties

  it('should equal true', function() {
    expect(true).to.equal(true);
  });

});
